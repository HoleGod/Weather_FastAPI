version: '3.9'

services:
  web:
    build: .
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    volumes:
      - .:/app
    ports:
      - 8002:8000
  
  tests:
    build: .
    command: pytest
    volumes:
      - .:/app